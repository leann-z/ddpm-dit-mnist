# ddpm-dit-mnist
DDPM implementation with a Diffusion Transformer (DiT) backbone on MNIST. Covers unconditional generation, class-conditional sampling, classifier-free guidance (w=1–20), and attention map visualisation across the denoising trajectory.
